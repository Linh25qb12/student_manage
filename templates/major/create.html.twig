{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    <div class="form-group">
        <label for="MajorName">MajorName:</label>
        {{ form_widget(form.name) }}
    </div>
    <input type="submit" name="create_major" value="Create" class="btn btn-primary"/>
    {{ form_end(form) }}
{% endblock %}